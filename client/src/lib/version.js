// App version - auto-injected from package.json at build time by Vite.
// To bump the version, update "version" in client/package.json (and
// server/package.json to keep them in sync). No manual edit needed here.
/* global __APP_VERSION__ */
export const APP_VERSION = __APP_VERSION__

// Set to true to force browsers to clear cached assets on this release.
// After confirming the deploy is stable, set back to false in the next patch.
export const FLUSH_CACHE = true
